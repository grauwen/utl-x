---
title: atan
description: "atan — UTL-X Math function. Arc tangent (inverse tangent). Returns angle in radians."
pageClass: stdlib-page
---

# atan

<p class="stdlib-meta"><code>atan(number) → number</code> · <a href="/reference/stdlib#math">Math</a></p>

Arc tangent (inverse tangent). Returns angle in radians.

- `number` (required): any numeric value

``` utlx
atan(0)        // 0.0
atan(1)        // 0.7853981633974483 (π/4)
atan(-1)       // -0.7853981633974483 (-π/4)
```
