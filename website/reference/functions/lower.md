---
title: lower
description: "lower — UTL-X String function. Alias for lowerCase(). Convert a string to all lowercase."
pageClass: stdlib-page
---

# lower

<p class="stdlib-meta"><code>lower(string) → string</code> · <a href="/reference/stdlib#string">String</a></p>

Alias for `lowerCase()`. Convert a string to all lowercase.

- `string` (required): the string to convert

``` utlx
{
  result: lower("Hello World")               // "hello world"
}
```
