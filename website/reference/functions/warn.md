---
title: warn
description: "warn — UTL-X System function. Log a value with WARN level and return it (passthrough)."
pageClass: stdlib-page
---

# warn

<p class="stdlib-meta"><code>warn(value) → value</code> · <a href="/reference/stdlib#system">System</a></p>

Log a value with WARN level and return it (passthrough).

- `value` (required): value to log and pass through

``` utlx
let result = warn("Deprecated field used")
```
