---
title: acos
description: "acos — UTL-X Math function. Arc cosine (inverse cosine). Returns angle in radians."
pageClass: stdlib-page
---

# acos

<p class="stdlib-meta"><code>acos(number) → number</code> · <a href="/reference/stdlib#math">Math</a></p>

Arc cosine (inverse cosine). Returns angle in radians.

- `number` (required): value between -1 and 1

``` utlx
acos(1)        // 0.0
acos(0)        // 1.5707963267948966 (π/2)
acos(-1)       // 3.141592653589793 (π)
```
