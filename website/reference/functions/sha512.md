---
title: sha512
description: "sha512 — UTL-X Security function. Compute a SHA-512 hash. Returns hex-encoded digest string. See Chapter"
pageClass: stdlib-page
---

# sha512

<p class="stdlib-meta"><code>sha512(data) → string</code> · <a href="/reference/stdlib#security">Security</a></p>

Compute a SHA-512 hash. Returns hex-encoded digest string. See Chapter
38.

- `data` (required): string to hash

``` utlx
sha512("hello")                          // "9b71d224bd62f378..."
{
  digest: sha512($input.document)
}
```
