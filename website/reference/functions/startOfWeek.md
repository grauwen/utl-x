---
title: startOfWeek
description: "startOfWeek — UTL-X Date & Time function. Get the start of the week (Monday) for the given date."
pageClass: stdlib-page
---

# startOfWeek

<p class="stdlib-meta"><code>startOfWeek(datetime) → datetime</code> · <a href="/reference/stdlib#date-time">Date & Time</a></p>

Get the start of the week (Monday) for the given date.

- `datetime` (required): a date or datetime value

``` utlx
startOfWeek(now())                       // Monday of current week
```
