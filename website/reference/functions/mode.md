---
title: mode
description: "mode — UTL-X Math function. Find the most frequently occurring value in a numeric array."
pageClass: stdlib-page
---

# mode

<p class="stdlib-meta"><code>mode(array) → number</code> · <a href="/reference/stdlib#math">Math</a></p>

Find the most frequently occurring value in a numeric array.

- `array` (required): array of numbers

``` utlx
{
  result: mode([1, 2, 2, 3, 3, 3])          // 3 (most frequent)
}
```
