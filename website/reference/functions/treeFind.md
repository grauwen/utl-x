---
title: treeFind
description: "treeFind — UTL-X Object function. Find a node by path in a tree structure."
pageClass: stdlib-page
---

# treeFind

<p class="stdlib-meta"><code>treeFind(object, path) → value</code> · <a href="/reference/stdlib#object">Object</a></p>

Find a node by path in a tree structure.

- `object` (required): tree structure

- `path` (required): path to search for

``` utlx
treeFind($input.org, "engineering.backend")
```
