---
title: toTitleCase
description: "toTitleCase — UTL-X String function. Convert a string to title case (capitalize first letter of each word)."
pageClass: stdlib-page
---

# toTitleCase

<p class="stdlib-meta"><code>toTitleCase(string) → string</code> · <a href="/reference/stdlib#string">String</a></p>

Convert a string to title case (capitalize first letter of each word).
Alias for `titleCase`.

- `string` (required): the string to convert

``` utlx
toTitleCase("hello world")               // "Hello World"
```
