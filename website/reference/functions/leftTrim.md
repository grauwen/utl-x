---
title: leftTrim
description: "leftTrim — UTL-X String function. Remove whitespace from the LEFT (start) of a string only."
pageClass: stdlib-page
---

# leftTrim

<p class="stdlib-meta"><code>leftTrim(string) → string</code> · <a href="/reference/stdlib#string">String</a></p>

Remove whitespace from the LEFT (start) of a string only.

- `string` (required): the string to trim

``` utlx
leftTrim("   hello   ")                  // "hello   "
```
