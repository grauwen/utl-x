#!/bin/bash
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
JAR="$SCRIPT_DIR/../build/libs/utlxd-1.3.0.jar"

if [ ! -f "$JAR" ]; then
    echo "Error: JAR not found at $JAR"
    echo "Run './gradlew :modules:daemon:jar' first"
    exit 1
fi

exec java -jar "$JAR" "$@"