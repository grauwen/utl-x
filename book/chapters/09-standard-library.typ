= Standard Library

== String Functions (83 functions)
// - Case: upperCase, lowerCase, capitalize, camelCase, snakeCase, kebabCase
// - Search: contains, startsWith, endsWith, indexOf, matches (regex)
// - Manipulation: trim, pad, replace, split, join, substring, repeat
// - Formatting: format, leftPad, rightPad, truncate

== Array Functions (67 functions)
// - Transform: map, flatMap, flatten, zip, unzip
// - Filter: filter, find, findIndex, every, some
// - Aggregate: reduce, sum, avg, min, max, count
// - Sort: sort, sortBy, reverse, unique, distinct
// - Access: first, last, take, drop, slice, chunk, groupBy

== Math Functions (37 functions)
// - Basic: abs, ceil, floor, round, sqrt, pow, log
// - Trigonometry: sin, cos, tan, asin, acos, atan
// - Constants: PI, E
// - Random: random, randomInt

== Date/Time Functions (68 functions)
// - Parse: parseDate, parseDateTime, parseTime
// - Format: formatDate, formatDateTime
// - Arithmetic: addDays, addMonths, addYears, dateDiff
// - Extract: year, month, day, hour, minute, second
// - Now: now, today

== Type Functions (27 functions)
// - Check: isString, isNumber, isBoolean, isArray, isObject, isNull
// - Convert: toString, toNumber, toBoolean
// - Inspect: getType, keys, values, length

== Encoding Functions (30 functions)
// - Base64: base64Encode, base64Decode
// - URL: urlEncode, urlDecode, urlEncodeComponent
// - HTML: htmlEncode, htmlDecode
// - Hex: hexEncode, hexDecode

== Security Functions (16 functions)
// - Hash: md5, sha1, sha256, sha512
// - HMAC: hmacSha256, hmacSha512
// - UUID: uuid, uuidV4

== XML Functions (60 functions)
// - Parse: parseXml, xpath
// - Encoding: detectXMLEncoding, convertXMLEncoding
// - Namespace: getNamespace, stripNamespaces
// - Validation: validateXML

== Other Categories
// - JSON (6): parseJson, renderJson
// - CSV (12): parseCsv, renderCsv, csvHeaders
// - YAML (22): parseYaml, renderYaml
// - Binary (47): compress, decompress, readBytes
// - Financial (16): formatCurrency, parseAmount, exchangeRate
// - Geospatial (8): distance, within, bbox
// - Utility (27): range, repeat, coalesce, default
