name: "missing_closing_brace"
category: "level1-syntax"
description: "Test detection of missing closing brace in object literal"
tags: ["syntax", "parse-error", "object", "brace"]

script: |
  %utlx 1.0
  input json
  output json
  ---
  {
    name: input.customerName,
    email: input.emailAddress

validation_expected:
  should_pass: false
  exit_code: 1

  errors:
    - level: "syntax"
      type: "parse_error"
      message_pattern: ".*(expected|missing|unterminated).*[})].*"
      severity: "error"

  warnings: []

metadata:
  author: "UTL-X Team"
  created: "2025-11-01"
