name: tools_list
description: Test MCP server tools/list endpoint returns all 6 tools
tags:
  - protocol
  - tools
  - list

sequence:
  - description: "Request list of available tools"
    request:
      jsonrpc: "2.0"
      id: 2
      method: "tools/list"
      params: {}
    expect:
      jsonrpc: "2.0"
      id: 2
      result:
        tools:
          - name: "get_input_schema"
            description: "{{STRING}}"
            inputSchema:
              type: "object"
              properties: "{{ANY}}"
          - name: "get_stdlib_functions"
            description: "{{STRING}}"
            inputSchema:
              type: "object"
              properties: "{{ANY}}"
          - name: "validate_utlx"
            description: "{{STRING}}"
            inputSchema:
              type: "object"
              properties: "{{ANY}}"
              required: ["utlx"]
          - name: "infer_output_schema"
            description: "{{STRING}}"
            inputSchema:
              type: "object"
              properties: "{{ANY}}"
              required: ["utlx"]
          - name: "execute_transformation"
            description: "{{STRING}}"
            inputSchema:
              type: "object"
              properties: "{{ANY}}"
              required: ["utlx", "input"]
          - name: "get_examples"
            description: "{{STRING}}"
            inputSchema:
              type: "object"
              properties: "{{ANY}}"
              required: ["query"]
