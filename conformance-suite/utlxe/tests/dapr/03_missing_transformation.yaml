name: "missing_transformation"
category: "dapr"
description: "Dapr delivers message for a binding with no uploaded transformation — expect 503 (not ready, Dapr retries)"
tags: ["dapr", "error-handling", "missing"]
transport: dapr

dapr:
  steps:
    - action: dapr_invoke_binding
      binding_name: "nonexistent-transform"
      input:
        format: json
        data: |
          {"test": true}
      expected:
        status: 503
