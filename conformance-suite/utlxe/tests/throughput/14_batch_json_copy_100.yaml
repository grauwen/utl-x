name: "batch_json_copy_100"
category: "throughput"
description: "100 varied JSON orders with COPY strategy — compare against TEMPLATE and COMPILED"
tags: ["throughput", "batch", "real-world", "copy", "100-instances"]

strategy: "COPY"

transformation_file: "../../../../examples/batch/json-orders/transform.utlx"

burst:
  count: 100
  mode: "concurrent"
  instances_dir: "../../../../examples/batch/json-orders/instances"
  content_type: "application/json"

throughput_limits:
  max_total_ms: 30000
