%utlx 1.0
input json
output json
---
{
  user: @input,
  computed: {
    display_name: @input.name,
    is_adult: @input.age >= 18,
    info: "User " + @input.name + " is " + toString(@input.age) + " years old"
  }
}